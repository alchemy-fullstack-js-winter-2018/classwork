export const getDogCreateName = state => {
  return state.dogCreate.name;
}

export const getDogCreateAge = state => {
  return state.dogCreate.age;
}

export const getDogCreateWeight = state => {
  return state.dogCreate.weight;
}
